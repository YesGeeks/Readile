server:
    port: 7856

spring:
    application:
        name: Readile
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/Readile?serverTimezone=UTC
        username: root
        password:
    jpa:
        hibernate:
            ddl-auto: update
    sql:
        init:
            mode: always